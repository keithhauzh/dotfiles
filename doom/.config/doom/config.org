#+TITLE: Config

* Table of Contents :toc:
- [[#appearance][Appearance]]
  - [[#theme][Theme]]
  - [[#transparent-background][Transparent Background]]
  - [[#relative-line-numbers][Relative line numbers]]
  - [[#cursor][Cursor]]
- [[#org][Org]]
  - [[#org-folder][Org Folder]]
  - [[#org-agenda][Org Agenda]]
  - [[#org-settings][Org Settings]]
- [[#flutter][Flutter]]
- [[#keybinds][Keybinds]]
- [[#dashboard][Dashboard]]

* Appearance
** Theme
#+begin_src emacs-lisp
(setq doom-theme 'doom-solarized-dark-high-constrast)
#+end_src
** Transparent Background
#+begin_src emacs-lisp
;; (add-to-list 'default-frame-alist '(alpha-background . 85))
#+end_src
** Relative line numbers
#+begin_src emacs-lisp
(setq display-line-numbers-type 'relative)
;; this makes it so relative line numbers will always show up
(add-hook 'after-change-major-mode-hook
          (lambda ()
            (when (and (not (minibufferp))
                       (not display-line-numbers-mode))
              (display-line-numbers-mode 1)))
          100)
#+end_src
** Cursor
*** Blinking Cursor
#+begin_src emacs-lisp
(blink-cursor-mode 1)
#+end_src
*** Uniform cursor in evil mode regardless or state
#+begin_src emacs-lisp
(after! evil
  (setq evil-insert-state-cursor 'box
        evil-normal-state-cursor 'box
        evil-visual-state-cursor 'box
        evil-replace-state-cursor 'box
        evil-motion-state-cursor 'box
        evil-emacs-state-cursor 'box
      )
)
#+end_src
* Org
** Org Folder
#+begin_src emacs-lisp
(setq org-directory "~/org")
#+end_src

** Org Agenda
#+begin_src emacs-lisp
(after! org
  (setq org-agenda-files "~/org/agenda.org"))
#+end_src

** Org Settings
#+begin_src emacs-lisp
;; (custom-theme-set-faces!
;;     'doom-monokai-spectrum'
;;     '(org-level-8 :inherit outline-3 :height 1.0)
;;     '(org-level-7 :inherit outline-3 :height 1.0)
;;     '(org-level-6 :inherit outline-3 :height 1.1)
;;     '(org-level-5 :inherit outline-3 :height 1.2)
;;     '(org-level-4 :inherit outline-3 :height 1.3)
;;     '(org-level-3 :inherit outline-3 :height 1.4)
;;     '(org-level-2 :inherit outline-2 :height 1.5)
;;     '(org-level-1 :inherit outline-1 :height 1.6)
;;     '(org-document-title :height 1.8 :bold t :underline nil)
;; )
#+end_src

* Flutter
#+begin_src emacs-lisp
(setenv "PATH" (concat "~/develop/sdk-flutter/bin:" (getenv "PATH")))
(add-to-list 'exec-path "~/develop/sdk-flutter/bin")

(setq lsp-dart-sdk-dir "~/develop/sdk-flutter/bin/cache/dart-sdk")
#+end_src

* Keybinds
#+begin_src emacs-lisp
(map! :leader
      :desc "save buffer"
      "e w" #'save-buffer
      )
(map! :leader
      :desc "Format buffer"
      "e f" #'+format/buffer
      )
#+end_src

* Dashboard
remove dashboard functions
#+begin_src emacs-lisp
(remove-hook '+doom-dashboard-functions #'doom-dashboard-widget-shortmenu)
#+end_src
